{
  "name": "cloud-panel",
  "version": "1.0.0",
  "description": "Starter dashboard panel with Express + PostgreSQL, ready to deploy on Railway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}
